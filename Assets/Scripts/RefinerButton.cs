using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class RefinerButton : MonoBehaviour
{
    //public bool buttonClicked = false;
    //public float clickableRadius = 0.5f;

    //public GameObject prefabUnrefinedCircle;
    ////private GameObject spawnedUnrefinedCircle;

    //public GameObject spawnedUnrefinedCircle;

    //private Camera gameCamera;

    //UnrefinedCircleScript unrefinedCircleScript;

    //public List<GameObject> unrefinedCircles = new List<GameObject>();

    // Start is called before the first frame update
    void Start()
    {

        //gameCamera = Camera.main;

        //unrefinedCircleScript = FindObjectOfType<UnrefinedCircleScript>();      
    }

    // Update is called once per frame
    void Update()
    {


        //Vector3 inputPositionInScreenSpace = gameCamera.WorldToScreenPoint(transform.position);

        //Vector3 inputPos = spawnedUnrefinedCircle.transform.position;
        //Vector3 refinerPos = transform.position;
        

        //float distanceBetween = Vector3.Distance(inputPos, refinerPos);



        //if (buttonClicked == true)
        //{
        //    if (distanceBetween <= clickableRadius)
        //    {
        //        Destroy(spawnedUnrefinedCircle);
        //        buttonClicked = false;

                
        //    }


        //}
    }



    //public void OnClick()
    //{
    //    buttonClicked = true;
        
    //}
}
